@model IEnumerable<Trazabilidad.Common.Models.Xad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VerXad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaXad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VerXad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaXad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.XadId }) |
            @Html.ActionLink("Details", "Details", new { id=item.XadId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.XadId })
        </td>
    </tr>
}

</table>
