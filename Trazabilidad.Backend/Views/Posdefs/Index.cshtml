@model IEnumerable<Trazabilidad.Common.Models.Posdef>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Posicion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Numman)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdEstacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Posicion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Numman)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdEstacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPosdef }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPosdef }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPosdef })
        </td>
    </tr>
}

</table>
