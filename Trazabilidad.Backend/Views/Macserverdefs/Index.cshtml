@model IEnumerable<Trazabilidad.Common.Models.Macserverdef>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.linea)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEstacion)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.linea)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEstacion)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Idmacserve }) |
                @Html.ActionLink("Details", "Details", new { id=item.Idmacserve }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Idmacserve })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
