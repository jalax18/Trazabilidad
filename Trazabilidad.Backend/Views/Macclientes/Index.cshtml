@model IEnumerable<Trazabilidad.Common.Models.Maccliente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VerMaccliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaMaccliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VerMaccliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaMaccliente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MacclienteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MacclienteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MacclienteId })
        </td>
    </tr>
}

</table>
